{% extends 'base.html.twig' %}
{% block title %}Home{% endblock %}

{% block body %}
    {% for post in posts %}
        {% if not app.user or app.user.id != post.user.id %}
            {{ include('post/_post.html.twig') }}
        {% endif %}
    {% endfor %}

    {% if app.user %}
        {% include 'partials/_new_post_button.html.twig' %}
    {% endif %}

    {% include 'post/index.html.twig' %}

{% endblock %}