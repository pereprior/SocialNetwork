{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <p>{{ post.content }}</p>
    {% if post.img %}
        <img src="{{ asset('uploads/' ~ post.img) }}" alt="{{ post.title }}">
    {% endif %}
{% endblock %}